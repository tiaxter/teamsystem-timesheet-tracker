export const CONFIG_DIRECTORY = `${Bun.env.HOME}/.timesheet-tracker`;

export const JSON_TIMESHEET_DIRECTORY = `${CONFIG_DIRECTORY}/json`;

export const JSON_TIMESHEET_DATE_FORMAT_FILENAME = 'MMMM-YYYY';

export const TYPOLOGIES_FILE = `${CONFIG_DIRECTORY}/typologies.toml`;

export const TIMESHEET_DIRECTORY = `${CONFIG_DIRECTORY}/timesheets`;
export const TEMPLATE_XLSX_FILE = `${CONFIG_DIRECTORY}/timesheet-template.xlsx`;
